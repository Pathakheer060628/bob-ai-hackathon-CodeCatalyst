# Presentation

TODO: add the slide deck (`slides.pdf` or similar) before final submission.

Suggested outline:

1. The problem — demand spikes + renewable curtailment, one operator desk
2. The pipeline — forecast → detect → root-cause → balance → minimize curtailment → verify
3. Live demo (see `../demo/screenshots/`)
4. The verifier agent — why the numbers can be trusted
5. Known limitations + what a production deployment would add
